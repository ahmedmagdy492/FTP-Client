@using FTP_Client.Helpers
@using FTP_Client.HelperModels
@inject IConfigReader configReader;

@{
    ViewData["Title"] = "Remote Session";
    string connectionID = ViewBag.ConnectionID;
    string ip = ViewBag.Ip;
    int port = ViewBag.Port;
    TempData["ConnectionID"] = connectionID;
}

<style>
    .max-height-card {
        max-height: 700px;
        min-height: 700px;
        overflow: auto;
    }

    .item {
        text-decoration: none;
        color: #000;
    }
</style>

<nav class="navbar navbar-light bg-light">
    <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
            <a class="nav-item item" href="@Url.Action("Index", "Home")">
                <img src="~/imgs/previous.png" width="20" height="20" /> <span class="mx-2">Back to Home</span>
            </a>
            <a href="javascript:void(0);" class="mx-2 item nav-item" onclick="enterFullScreen()">
                <img src="~/imgs/full-screen.png" width="20" height="20" /> <span class="mx-2">Enter Full Screen</span>
            </a>
        </li>
    </ul>
</nav>

<section id="connection-section">
    <div class="row mt-4">
        <div class="col-md-12">
            <h4> Connected to @ip:@port</h4>
        </div>
    </div>

    <hr class="my-2" />

    <div class="row mt-3">
        <div class="col-md-6">
            <div class="card max-height-card">
                <div class="card-header">
                    <div class="d-flex align-items-center justify-content-between mb-2">
                        <h4 class="card-title">Your Computer</h4>
                        <button class="btn btn-primary">Upload</button>
                    </div>
                    <input type="text" class="form-control" value="/" readonly id="fullPathInput" />
                </div>
                <div class="card-body" id="localContainer">
                    <h4 class="text-center mt-3">Loading...</h4>
                </div>
            </div>
        </div>
        <div class="col-md-6">
            <div class="card max-height-card">
                <div class="card-header">
                    <div class="d-flex align-items-center justify-content-between mb-2">
                        <h4 class="card-title">Remote Computer - @ip</h4>
                        <button class="btn btn-primary">Downlaod</button>
                    </div>
                    <input type="text" class="form-control" value="" readonly />
                </div>
                <div class="card-body" id="remoteContainer">
                    <h4 class="text-center mt-3">Loading...</h4>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts{
<script src="~/js/Custom/Session.js"></script>
<script>
    window.addEventListener('load', function () {
        navigateRemote("@connectionID", "\\");
        navigateLocal("@connectionID", "");
    });
</script>
}